from setuptools import setup, find_packages

setup(
    name="pyunluac",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
    description="Python library to decompile Lua bytecode (.luac)",
    author="Your Name",
)
